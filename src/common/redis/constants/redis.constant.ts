/**
 * Injection token for the Redis Keyv cache client provider.
 * Used to inject the shared Redis cache connection across the application.
 */
export const RedisClientCachedProvider = 'RedisClientCachedProvider';
