export const FeatureFlagDefaultAvailableSearch = ['key'];
/**
 * Cursor-route allow-list. `createdAt` and `key` are written when the row is seeded and no update
 * path touches either, so neither key moves a row mid-scroll.
 */
export const FeatureFlagDefaultAvailableOrderBy = ['createdAt', 'key'];
