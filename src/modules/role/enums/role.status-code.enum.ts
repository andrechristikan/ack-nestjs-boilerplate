/**
 * Status codes raised by the `role` module.
 * @public
 */
export enum EnumRoleStatusCodeError {
    notFound = 50500,
    exist = 50501,
    predefinedNotFound = 50502,
    forbidden = 50503,
    used = 50504,
}
