# Build dependencies (keep node_modules for package.json but exclude if exists)
node_modules/
coverage/
dist/
src/swagger.json
.husky/
.github/
assets/
src/metadata.ts
keys/*
.warmup/
/.nyc_output

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment (contains sensitive data - but keep .env.example)
.env*
!.env.example
config.yaml
config.yml

# OS files
.DS_Store

# Versioning and metadata
.git
.gitignore
.dockerignore

# IDE and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Yarn PnP
.yarn/*
.pnp.*
!.yarn/cache
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Files not required for Docker build
.editorconfig
cspell.json
README.md
LICENSE.md
nodemon.json

# CI configuration (except ci/dockerfile which is used for production build)
ci/jwks-server/

# Keep these files as they are needed for production build:
# - ci/dockerfile (used by GitHub Actions)
# - eslint.config.mjs (might be needed for linting)
# - nest-cli.json (needed for NestJS CLI commands)  
# - tsconfig.json (needed for TypeScript compilation)
# - test/ (keep for testing capabilities)
# - scripts/ (might contain build scripts)